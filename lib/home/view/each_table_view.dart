import 'package:flutter/material.dart';

class EachTableView extends StatelessWidget {
  const EachTableView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
